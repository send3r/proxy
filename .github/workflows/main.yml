name: Temporary SOCKS5 Proxy (ngrok TCP + Chisel)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Proxy süresi (1h, 2h, 4h, 6h)'
        required: true
        default: '2h'

jobs:
  proxy:
    runs-on: ubuntu-latest

    steps:
      - name: Install Chisel
        run: |
          wget -q https://github.com/jpillora/chisel/releases/latest/download/chisel_1.11.5_linux_amd64.gz -O chisel.gz
          gzip -d chisel.gz
          sudo mv chisel /usr/local/bin/chisel
          chmod +x /usr/local/bin/chisel
          echo "✅ Chisel installed: $(chisel --version)"

      - name: Install ngrok
        run: |
          curl -sSL https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update && sudo apt install -y ngrok
          echo "✅ ngrok installed: $(ngrok --version)"

      - name: Configure ngrok & Start Proxy
        env:
          NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
        run: |
          if [ -z "$NGROK_AUTHTOKEN" ]; then
            echo "❌ HATA: NGROK_AUTH_TOKEN secret'i eklenmemiş!"
            echo "Lütfen GitHub repo → Settings → Secrets and variables → Actions → New repository secret oluştur."
            exit 1
          fi

          echo "🔑 ngrok authtoken ayarlanıyor..."
          ngrok config add-authtoken $NGROK_AUTHTOKEN

          # Chisel SOCKS5'i arka planda başlat
          chisel server --socks5 --port 1080 > chisel.log 2>&1 &
          echo "✅ Chisel SOCKS5 server başlatıldı (port 1080)"

          echo "🚀 ngrok TCP tunnel başlatılıyor..."
          echo "=== Proxifier için bilgileri bekle ==="
          timeout ${{ github.event.inputs.duration }} ngrok tcp 1080
